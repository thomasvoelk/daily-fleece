name: Frontend CI

on:
  push:
    paths:
      - 'frontend/**'
      - 'api/**'
  pull_request:
    paths:
      - 'frontend/**'
      - 'api/**'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Set up Node.js
        uses: actions/setup-node@v5
        with:
          node-version: '22'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: frontend

      - name: Generate API client
        run: npm run generate:api
        working-directory: frontend

      - name: Build
        run: npm run build
        working-directory: frontend

      - name: Check formatting
        run: npx prettier --check "src/**/*.{ts,html,json}" "../api/**/*.yaml"
        working-directory: frontend

      - name: Lint
        run: npm run lint
        working-directory: frontend

      - name: Test
        run: npm test
        working-directory: frontend
